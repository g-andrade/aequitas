[
 {aequitas,
  [{categories,
    #{ static_configuration_categoryA => [{max_window_size, 10}],
       static_configuration_categoryB => [{max_window_size, 42}],
       {static_configuration, non_atom_category} => [{max_window_size, 999}]
     }}
  ]}
].
